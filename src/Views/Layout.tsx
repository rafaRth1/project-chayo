import Header from './Header/Header';
import Main from './Main';

const Layout = () => {
   return (
      <div className='relative'>
         <Header />
         <Main />
      </div>
   );
};

export default Layout;
